import {PersonDTO} from './PersonDTO';

export class ShutterDTO{
  status: string;
  state: string;
  minvalueref: string;
  suspect: string;
  maxvalueref: string;
  percentage: string;
  person: PersonDTO;
}

