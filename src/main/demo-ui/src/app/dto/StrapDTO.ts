import {PersonDTO} from './PersonDTO';

export class StrapDTO{
  id: bigint;
  status: string;
  state: string;
  minvalueref: string;
  suspect: string;
  price: number;
  ipadress: string;
  maxvalueref: string;
  person: PersonDTO;
}

