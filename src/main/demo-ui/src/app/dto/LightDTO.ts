import {PersonDTO} from './PersonDTO';

export class LightDTO{
  status: string;
  state: string;
  minvalueref: string;
  suspect: string;
  maxvalueref: string;
  percentage: string;
  color: string;
  person: PersonDTO;
}

