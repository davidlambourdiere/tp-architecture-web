import {PersonDTO} from './PersonDTO';

export class IOTDTO{
  status: string;
  state: string;
  startdate: string;
  suspect: string;
  minvalueref: string;
  maxvalueref: string;
  activityduration: string;
  person: PersonDTO;
}

